#

## Klein Prompt

In the context that you gave above, can you do the following:

- create a skill that would decide, based on the type of image required, whether klein or sdxl should be used for image generation. If it's difficult to know which one would be better, the skill should just recommend trying both: generating the image using both the tools and then comparing.
- Create a document that the AI agent can use as a reference point of what the differences are that would help decide whether klein or sdxl should be used for image generation

# Best Image models

The cost doesn't matter to me as much as the quality of the images generated. Since I'll be publishing more than a hundred books, it might be better to get a license to generate the best images only if the result is much better than SDXL and Flux, both for the book cover and for the inner images, both colored and grayscale.

What are the best options I have to generate images locally with ComfyUI or a similar app? I'm willing to pay as long as the cost is not too much and the result is much better than SDXL and Flux
